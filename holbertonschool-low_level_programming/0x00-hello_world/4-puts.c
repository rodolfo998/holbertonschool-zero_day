#include <stdio.h>
/**
 * main - Entry point
 * Uses put to add piece of quote
 * Return: Always 0 (ok)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
